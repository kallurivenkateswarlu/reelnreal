import React, { useState } from 'react';
import { Film, TrendingUp, Newspaper, History, Bookmark, Settings, Search, Flame, Menu, X, Home, MapPin, Trophy, BriefcaseBusiness, Cpu, Clapperboard, Heart } from 'lucide-react';
import { NavTab, Language, WordPressConfig } from '../types';

interface NavbarProps { activeTab: NavTab; setActiveTab: (tab: NavTab) => void; language: Language; setLanguage: (lang: Language) => void; searchQuery: string; setSearchQuery: (q: string) => void; bookmarksCount: number; onOpenSettings: () => void; wpConfig: WordPressConfig; onNavigate?: (path: string) => void; currentPath?: string; }

const menu = [
  ['/','Home',Home], ['/movie-updates','Movie Updates',Film], ['/national-news','National News',Newspaper], ['/local-news','Local News',MapPin], ['/andhra-pradesh','Andhra Pradesh',MapPin], ['/politics','Politics',Newspaper], ['/sports','Sports',Trophy], ['/business','Business',BriefcaseBusiness], ['/technology','Technology',Cpu], ['/entertainment','Entertainment',Clapperboard], ['/lifestyle','Lifestyle',Heart], ['/trending','Trending',Flame],
] as const;

export const Navbar: React.FC<NavbarProps> = ({ activeTab, setActiveTab, language, setLanguage, searchQuery, setSearchQuery, bookmarksCount, onOpenSettings, wpConfig, onNavigate, currentPath = '/' }) => {
  const [open, setOpen] = useState(false); const isTe = language === 'te';
  const go = (path: string) => { setOpen(false); onNavigate ? onNavigate(path) : setActiveTab(path === '/movie-updates' ? 'movies' : 'news'); };
  return <header className="sticky top-0 z-40 bg-zinc-950/95 backdrop-blur-md border-b border-zinc-800 shadow-2xl"><div className="bg-amber-500/10 border-b border-amber-500/20 py-1 px-4 text-xs text-amber-300 flex items-center gap-3 overflow-hidden"><Flame className="w-3.5 h-3.5 text-amber-500 shrink-0" /><span className="font-black uppercase tracking-wider text-[10px] shrink-0">{isTe ? 'తాజా వార్త' : 'BREAKING NEWS'}</span><span className="truncate">Politics • Sports • Technology • Entertainment • Telugu Cinema</span></div><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-3"><div className="flex items-center justify-between gap-4"><a href="/" onClick={e => { e.preventDefault(); go('/'); }} className="flex items-center gap-3 shrink-0 group"><img src="/reelnreal-logo.svg" alt="Reel N Real" className="w-11 h-11 object-contain" /><span><strong className="font-serif-display text-xl sm:text-2xl font-black text-white group-hover:text-amber-400">REEL <b className="text-amber-500">N</b> REAL</strong><small className="block text-[9px] uppercase tracking-[0.18em] text-zinc-400">News • Movies • Culture</small></span></a><div className="hidden md:flex flex-1 max-w-md relative mx-4"><Search className="absolute left-3.5 top-1/2 -translate-y-1/2 w-4 h-4 text-zinc-400" /><input value={searchQuery} onChange={e => setSearchQuery(e.target.value)} placeholder="Search articles, movies, news..." className="w-full bg-zinc-900 text-sm rounded-full pl-10 pr-4 py-2.5 border border-zinc-800 focus:outline-none focus:border-amber-500" /></div><div className="flex items-center gap-2"><div className="hidden sm:flex bg-zinc-900 border border-zinc-800 rounded-lg p-1 text-xs font-semibold"><button onClick={() => setLanguage('en')} className={`px-2.5 py-1 rounded-md ${language === 'en' ? 'bg-amber-500 text-zinc-950' : 'text-zinc-400'}`}>EN</button><button onClick={() => setLanguage('te')} className={`px-2.5 py-1 rounded-md ${language === 'te' ? 'bg-amber-500 text-zinc-950' : 'text-zinc-400'}`}>తెలుగు</button></div><button onClick={onOpenSettings} className="hidden sm:flex p-2 rounded-lg border border-zinc-800 text-zinc-400 hover:text-amber-400"><Settings className="w-4 h-4" /></button><button onClick={() => setOpen(!open)} className="md:hidden p-2 rounded-lg border border-zinc-800 text-zinc-200" aria-label="Toggle navigation">{open ? <X /> : <Menu />}</button></div></div><div className="mt-3 md:hidden relative"><Search className="absolute left-3.5 top-1/2 -translate-y-1/2 w-4 h-4 text-zinc-400" /><input value={searchQuery} onChange={e => setSearchQuery(e.target.value)} placeholder="Search articles, movies, news..." className="w-full bg-zinc-900 text-sm rounded-lg pl-10 pr-4 py-2.5 border border-zinc-800 focus:outline-none focus:border-amber-500" /></div><nav className={`${open ? 'flex' : 'hidden'} md:flex flex-wrap items-center gap-1 mt-3 pt-2 border-t border-zinc-800/60`}><div className="flex flex-wrap gap-1">{menu.map(([path,label,Icon]) => <button key={path} onClick={() => go(path)} className={`flex items-center gap-1.5 px-2.5 py-2 rounded-lg text-xs font-bold ${currentPath === path ? 'bg-amber-500 text-zinc-950' : 'text-zinc-400 hover:bg-zinc-900 hover:text-white'}`}><Icon className="w-3.5 h-3.5" />{label}</button>)}<button onClick={() => setActiveTab('bookmarks')} className={`flex items-center gap-1.5 px-2.5 py-2 rounded-lg text-xs font-bold ${activeTab === 'bookmarks' ? 'bg-amber-500 text-zinc-950' : 'text-zinc-400 hover:bg-zinc-900'}`}><Bookmark className="w-3.5 h-3.5" />Saved {bookmarksCount > 0 && `(${bookmarksCount})`}</button></div></nav></div></header>;
};
